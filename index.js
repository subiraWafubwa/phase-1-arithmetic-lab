// Define num1 and num2 such that their product is 62
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1;

// Define num3 and num4 such that their remainder is 4 when num3 is divided by num4
const num3 = 28;
const num4 = 6;
const mod = num3 % num4;

// Define a set of numbers and return the highest number
const max = Math.max(1, 5, 10, 20);